<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cbb.wasteRecovery.dao.StationDao">
<resultMap type="org.cbb.wasteRecovery.bean.Station" id="tion">
  <id property="id" column="id"/>
  <result property="money" column="money"/>
  <result property="name" column="name"/>
  <result property="address" column="address"/>
  <result property="locationY" column="locationY"/>
  <result property="locationX" column="locationX"/>
  <result property="fullAdd" column="fullAdd"/>
  <result property="geohash" column="geohash"/>
</resultMap>



    <select id="selectById" resultType="Station" parameterType="int">
        select name,address,detailed_address
        from station
        where
           id=#{id}
       
        
    </select>


    
    <select id="selectByAdd" resultType="Station" >
        select name,id,detailed_address
        from station
        where
        address like '%' #{address} '%'
        order BY id
        limit #{offset},#{limit}
       
        
    </select>
    
    <select id="selectByName" resultType="Station">
        select address,id,detailed_address
        from station
        where
           name=#{name}
        order BY id
        limit #{offset},#{limit}
       
        
    </select>

    <insert id="insertStaion">

        insert  into station(name,address,detailed_address,position)
        values (#{name},#{address},#{detailed_address},point(#{locationX},#{locationY}))
    </insert>


    <delete id="deleteStation">
        delete
        from
        station
        where
        id=#{id}

    </delete>
    
    <update id="updateData">
       
        update
          station
        set

          name = #{name},
         address = #{address},
         detailed_address = #{detailed_address}

        where
          id = #{id}
      
    </update>
    
</mapper>