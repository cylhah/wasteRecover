<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cbb.wasteRecovery.dao.OrderformDao">
<resultMap type="org.cbb.wasteRecovery.bean.Orderform" id="order">
  <id property="id" column="id"/>
  <result property="uid" column="uid"/>
  <result property="cid" column="cid"/>
  <result property="eid" column="eid"/>
  <result property="aid" column="aid"/>
  <result property="staid" column="staid"/>
  <result property="state" column="state"/>
  <result property="weight" column="weight"/>
  <result property="createTime" column="createTime"/>
  <result property="appointSTime" column="appointSTime"/>
  <result property="appointETime" column="appointETime"/>
  <result property="price" column="price"/>
  <collection property="user" resultMap="org.cbb.wasteRecovery.dao.UserDao.User"/>
  <collection property="collector" resultMap="org.cbb.wasteRecovery.dao.CollectorDao.Collect"/>
  <collection property="employee" resultMap="org.cbb.wasteRecovery.dao.EmployeeDao.Employ"/>
  <collection property="station" resultMap="org.cbb.wasteRecovery.dao.StationDao.tion"/>
  <collection property="scraptypeList" resultMap="org.cbb.wasteRecovery.dao.ScraptypeDao.type"/>
  <collection property="address" resultMap="org.cbb.wasteRecovery.dao.UserAddressDao.Address"/>
</resultMap>
    <insert id="insertOrderform">

    insert  into orderform(state,createTime,appointTime,aid,uid)
    values (#{state},#{createTime},#{appointTime},#{aid},#{uid})

    </insert>


    <sql id="columns">state,weight,createTime,appointTime,updateTime,price,aid,uid,cid</sql>

     <select id="selectById"  resultType="Orderform" parameterType="long">
        select <include refid="columns"/>
        from orderform

        where
           id=#{id}
       
        
    </select>
    
    <select id="selectByCId" resultType="Orderform">
        select <include refid="columns"/>
        from orderform

        where
           cid=#{cid}
        and  state=#{state}
        order BY id
        limit #{offset},#{limit}
       
        
    </select>
    
    <select id="selectByUId" resultType="Orderform">
        select <include refid="columns"/>
        from orderform

        where
          uid=#{uid}
        and  state=#{state}
        order BY id
        limit #{offset},#{limit}
        
    </select>
    

    

    
    
    <select id="selectByState" resultType="Orderform">
        select <include refid="columns"/>
        from orderform

        where
           state=#{state}
        order BY id
        limit #{offset},#{limit}
        
    </select>
    
    <select id="selectByPositon" resultMap="order">
        select o.state,o.weight,o.createTime,o.appointTime,o.updateTime,o.price,o.aid,o.uid,o.cid,d.uid,d.address,d.detail,d.community_id,d.geohash,d.position
        from orderform o
        INNER JOIN useraddress d
        where
        Math.sqrt(toRadians(d.locationX-#{locationX}) * 6367000.0* Math.cos(toRadians((d.locationY+#{locationY})/2.0)) * toRadians(d.locationX-#{locationX}) * 6367000.0* Math.cos(toRadians((d.locationY+#{locationY})/2.0)) + 6367000.0 * toRadians(d.locationY-#{locationY}) * 6367000.0 * toRadians(d.locationY-#{locationY}))  <![CDATA[ <= ]]>#{distance}

       and d.geohash LIKE CONCAT(?, '%');
        order BY o.id
        limit #{offset},#{limit}
        
    </select>

    

    

    
    <update id="updateOrderform">
       
        update
          orderform
        set
          aid = #{aid},
          appointTime=#{appointTime}
        where
          id = #{id}
      
    </update>
    
    <update id="submitData">
       
        update
          orderform
        set
          uid= #{uid},
          cid = #{cid},
          aid= #{aid},
          price = #{price},
          state = #{state},
          weight = #{weight}
        where
          id = #{id}
      
    </update>
    
    <delete id="deleteOrderform">
       delete
         from
         orderform
       where
           id=#{id}
    
    </delete>
</mapper>