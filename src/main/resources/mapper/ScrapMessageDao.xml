<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cbb.wasteRecovery.dao.ScrapMessageDao">

    <insert id="insertScrapMessage">

        insert  into scrapmessage(weight,price,oid,scrapid)
        values (#{weight},#{price},#{oid},#{scrapid})
    </insert>


    <delete id="deleteScrapMessage">
        delete
        from
        scrapmessage
        where
        oid=#{oid}
        and scrapid=#{scrapid}

    </delete>


    <update id="updateData">

        update
        scrapmessage
        set

        weight = #{weight}
        and  price = #{price}
        where
        scrapid = #{scrapid}
         and oid= #{oid}
    </update>



    <select id="selectByOId" resultType="ScrapMessage" >
        select weight,price,oid,scrapid
        from scrapmessage
        where
       oid= #{oid}


    </select>

    <select id="selectByScId" resultType="ScrapMessage" >
        select weight,price,oid,scrapid
        from scrapmessage
        where
        scrapId= #{scrapId}


    </select>

</mapper>