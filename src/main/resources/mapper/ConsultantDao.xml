<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cbb.wasteRecovery.dao.ConsultantDao">

    <insert id="insertConsultant">

        insert  into consultant(staid,password,username)
        values (#{staid},#{password},#{username})
    </insert>


    <delete id="deleteConsultant">
        delete
        from
        consultant
        where
        id=#{id}

    </delete>

    <update id="updateStaId" >

        update
      consultant
        set
        staid=#{staid}

        where
        id=#{id}

    </update>

    <update id="updatePassword" >

        update
        consultant
        set
        password=#{password}

        where
        id=#{id}

    </update>

    <select id="selectByUsername" resultType="Consultant" parameterType="String">
        select id,staid,password,username
        from consultant
        where
        username=#{username}


    </select>

    <select id="selectByUsernameAndPass" resultType="Consultant" >
        select id,staid,password,username
        from consultant
        where
        username=#{username}
        and password=#{password}


    </select>


    <select id="selectByStaId" resultType="Consultant" >
        select id,staid,password,username
        from consultant
        where
        staid=#{staid}
        order BY id
        limit #{offset},#{limit}


    </select>
</mapper>