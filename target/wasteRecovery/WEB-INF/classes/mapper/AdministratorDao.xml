<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.cbb.wasteRecovery.dao.AdministratorDao">
<resultMap type="org.cbb.wasteRecovery.bean.Administrator" id="Admin">
  <id property="id" column="id"/>
  <result property="staid" column="staid"/>
  <result property="username" column="username"/>
  <result property="password" column="password"/>
  <collection property="employeeList" resultMap="org.cbb.wasteRecovery.dao.EmployeeDao.Employ"/>

</resultMap>
   <select id="selectAdmin" resultType="org.cbb.wasteRecovery.bean.Administrator" parameterType="org.cbb.wasteRecovery.bean.Administrator">
        select employeeList
        from administrator
        where
           username=#{administrator.getUsername()}
        and password=#{administrator.getPassword()}   
        order by id
    </select>
    
    <insert id="insertAdmin">
        
        insert  into administrator(staid,username,password)
        values (#{administrator.getStaid()},#{administrator.getUsername()},#{administrator.getPassword()})
    </insert>
    
    <update id="updatePassword">
        <!-- 具体sql -->
        update
          employee
        set
          password = #{employee.getPassword()}
        where
          username = #{employee.getUsername()}
      
    </update>
</mapper>